apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: backend
  namespace: backend-namespace
spec:
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 5001
  type: NodePort
  selector:
    app: backend