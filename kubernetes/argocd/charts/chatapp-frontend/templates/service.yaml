apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatapp-frontend.fullname" . }}
  labels:
    {{- include "chatapp-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default "http" }}
      protocol: TCP
      name: http
  selector:
    {{- include "chatapp-frontend.selectorLabels" . | nindent 4 }}
