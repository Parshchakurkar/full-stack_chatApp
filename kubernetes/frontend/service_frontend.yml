apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend
  namespace: frontend-namespace
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
  type: LoadBalancer
  selector:
    app: frontend